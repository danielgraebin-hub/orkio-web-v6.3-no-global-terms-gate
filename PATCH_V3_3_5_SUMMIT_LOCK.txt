ORKIO WEB PATCH v3.3.5 — SUMMIT LOCK

Included fixes:
- src/ui/api.js now auto-serializes plain JSON bodies
- apiFetch() now returns { data } to match existing frontend callers
- build validated with npm ci + npm run build

Purpose:
- stabilize login, /api/me, onboarding, threads, messages, and admin data fetches before Summit
